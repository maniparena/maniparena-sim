# QUANTA_X1 SDK ROS2 (2D lidar and cameras enabled). No data recording.

# Keyboard chassis sensitivity (summed with /chassis/cmd_vel).
keyboard:
  linear_velocity: 0.5
  angular_velocity: 2.0

# ROS2 bridge.
ros:
  use_sim_time: true
  control_rate_hz: 10.0
  cmd_vel_timeout_s: 0.25
