# Deprecated alias of vuer.yaml. OpenXR is removed; --control-mode vr now uses Vuer.
enable_cameras: true
num_envs: 1
num_demos: 10
step_hz: 20
max_steps: 0
working_path: "~/maniparena_output/recordings/"
async_writer: false
stager_chunk_size: 32
teleop_config:
  vuer_port: 8012
