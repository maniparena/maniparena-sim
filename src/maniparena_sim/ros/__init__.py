"""ROS2 navigation bridge for the ex001 whole-body robot (2D-lidar nav).

Isolated subpackage: imports ``rclpy`` / Isaac ROS2 bridge extensions lazily at
runtime. Nothing outside this package should import it at module load time.
"""
